# AGENTS.md

> このリポジトリを編集する AI / 自動エージェント向けの仕様書です。  
> 人間開発者が読むことも想定しています。

---

## 1. プロジェクト概要

- 名前: **Prompt Snippet Manager for ChatGPT (Chrome Extension)**
- 目的:
  - ChatGPT / chatgpt.com 上でよく使うプロンプトを **スニペットとして登録・管理** し、
  - 右下のフローティングボタンから **ワンクリックで入力欄に挿入** できるようにすること。
- 想定ユーザー:
  - プロンプトを使い回したいエンジニア / ライター / 研究者など。
  - 英語 UI / 日本語 UI どちらの ChatGPT ユーザーも対象。

---

## 2. 技術スタック / 実行環境

- **Chrome 拡張機能 (Manifest V3)**
  - `manifest_version: 3`
- 言語:
  - フロントエンド: **HTML / CSS / JavaScript (ES2015+)**
- ストレージ:
  - `chrome.storage.sync` を使用  
    → 複数端末間でスニペットが同期される前提。

---

## 3. 現在のフォルダ構成（予定）

> ここに書かれたパス／ファイル名を前提としてコードを編集してください。

```text
.
├─ manifest.json
├─ AGENTS.md               # ← このファイル
├─ popup.html              # スニペット管理 UI（拡張アイコンを押したときのポップアップ）
├─ popup.js
├─ popup.css
├─ contentScript.js        # ChatGPT ページに注入されるスクリプト
├─ contentScript.css
└─ icons/
   ├─ icon16.png
   ├─ icon32.png
   ├─ icon48.png
   └─ icon128.png
```

将来的に以下を追加する可能性があります（存在しない場合は勝手に参照しないこと）:

- `README.md`
- `LICENSE`
- `src/` ディレクトリ（モジュール分割を行う場合）

---

## 4. 機能要件（必須）

### 4.1 スニペット管理（popup 側）

- 拡張アイコンをクリックすると `popup.html` が開く。
- Popup では以下の情報を入力できる:
  - タイトル `title`（必須）
  - カテゴリ `category`（任意・文字列）
  - タグ `tags`（任意・カンマ区切り → 配列に変換）
  - 本文 `body`（必須）
  - お気に入り `favorite`（boolean）
- スニペット一覧の表示:
  - タイトル・カテゴリ・タグ・お気に入り状態を表示。
  - Edit / Delete ボタンで編集・削除可能。
- フィルタ:
  - フリーテキスト検索（タイトル / カテゴリ / タグ / 本文を対象に部分一致）
  - カテゴリ絞り込み
  - お気に入りのみ表示

### 4.2 ChatGPT 上での利用（content script 側）

- ChatGPT / chatgpt.com のページ右下に **フローティングボタン** を表示する。
- ボタンを押すと、以下を満たす **パネル UI** が開閉される:
  - タイトル: `Prompt Snippets`
  - カテゴリ選択プルダウン
  - 検索欄
  - お気に入りのみ切替ボタン
  - スニペット一覧（タイトル＋カテゴリ＋タグ）
- スニペットをクリックすると:
  - ChatGPT の **入力欄（textarea または contenteditable）に本文が挿入**される。
  - 既に文字がある場合は、**カーソル位置** に挿入するのが理想。
  - React ベースの入力欄であっても、`input` イベントを発火させて内部状態が同期されること。
- Popup 側と content script 側は **同じ `chrome.storage.sync` キー** を参照すること:
  - 今のキー名: `"promptSnippets"`

---


## 5. コーディング規約 / 方針

エージェントがコードを書くときは、以下を可能な範囲で守ってください。

1. **依存ライブラリは追加しない**
   - 純粋な vanilla JS / HTML / CSS で実装すること。
2. **Chrome 拡張のベストプラクティスを守る**
   - コンテンツスクリプトから `chrome.*` API を直接使用してよい。
   - `eval` やインラインスクリプトは使用しない。
3. **状態は極力シンプルに**
   - 追加の状態管理ライブラリなどは導入しない。
   - 必要な状態はローカル変数と DOM から再構築できるようにする。
4. **命名**
   - 変数名・関数名は英語（lowerCamelCase）。
   - 日本語コメントは歓迎。
5. **ログ出力**
   - デバッグ用の `console.log` は、PR 作成時には極力削除すること。
   - どうしても残す場合は、`// TODO: remove debug log` を付ける。

---

## 6. テスト方法（ローカル開発者 / エージェント向け）

1. このリポジトリを clone / pull する。
2. Chrome で `chrome://extensions/` を開く。
3. 右上の「**デベロッパーモード**」を ON にする。
4. 「**パッケージ化されていない拡張機能を読み込む**」 → このリポジトリのフォルダを指定。
5. ChatGPT ( `https://chatgpt.com/` または `https://chat.openai.com/` ) を開き、以下を確認:
   - 右下にフローティングボタンが表示されること。
   - Popup でスニペットを登録すると、パネルに反映されること。
   - スニペットをクリックすると入力欄に挿入されること。

---

## 7. エージェントへの作業指示テンプレート

人間開発者が AI に指示を出すときは、可能であれば以下の形式を使ってください。

```text
【目的】
〜〜〜〜〜〜〜〜〜〜〜

【対象ファイル】
- contentScript.js

【やってほしいこと】
- ChatGPT の新 UI でも確実に入力欄を特定して、スニペット本文を挿入できるように関数を修正してください。
- 既存の public API（chrome.storage など）は変えずに、中身のみ改善してください。

【制約】
- 新しい外部ライブラリを追加しないこと。
- 既存の関数シグネチャは維持すること（呼び出し側を壊さない）。
```

---

## 8. 今後の拡張アイデア（参考・必須ではない）

- ショートカットキーで特定スニペットを挿入する機能（例: `Alt+1`〜`Alt+9`）。
- スニペットのインポート / エクスポート（JSON）。
- 多言語 UI（少なくとも日本語 / 英語の切り替え）。
- 将来的な Pro 版を見据えた設計（スニペット数の上限など）。

---

この `AGENTS.md` は、**エージェント／AI にとっての「README 兼仕様書」** です。  
仕様が変わった場合は、人間開発者がここを更新し、  
「どの振る舞いが正としてほしいか」を常に明示してください。

