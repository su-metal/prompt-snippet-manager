# Free vs Pro Plan Specification (Prompt Snippet Manager)

このドキュメントは、Prompt Snippet Manager（Chrome 拡張）の **無料版（Free）と有料版（Pro）の仕様差分** をまとめたものです。

---

## 1. 共通コンセプト

- ChatGPT / chatgpt.com 上で使う **プロンプトスニペット管理ツール**。
- スニペットを登録 → 右下パネルからワンクリックで挿入 → プレースホルダは `{{name}}` 記法。
- Free / Pro どちらも **基本の UX（登録・検索・挿入）は同じ** で、機能上の制限で差別化する。

---

## 2. コア機能の違い

### 2.1 スニペット数

- **Free**
  - 上限: **5 個まで**
  - 6個目を新規保存しようとした場合、保存をブロックし、上限到達メッセージを表示。
  - 既存スニペットの「編集」は、上限に関係なく許可（件数が増えないため）。

- **Pro**
  - 上限: **無制限**

---

### 2.2 プレースホルダ（`{{name}}`）

> ✅ 今回の重要な仕様変更：**Free は「1スニペットにつき 1種類のプレースホルダ名だけ」使用可能。**

- プレースホルダの書き方:
  - `{{topic}}` / `{{role}}` のように `{{}}` で名前を囲む。

- **Free**
  - 1つのスニペットで使える **プレースホルダ名の種類は 1つまで**。
  - 同じ名前は何回使ってもよい：
    - ✅ OK 例: `{{topic}} について調べてください。`  
      → プレースホルダ名は `topic` の1種類だけ。
    - ✅ OK 例: `あなたは {{role}} です。以後 {{role}} として答えてください。`  
      → プレースホルダ名は `role` の1種類だが複数回登場。
    - ❌ NG 例: `あなたは {{role}} です。{{topic}} について教えてください。`  
      → `role` と `topic` の2種類が含まれるため、Free では保存時に制限に引っかかる。
  - 実装イメージ: 保存時に本文 `body` からプレースホルダ名を抽出し、ユニークな名前の数をカウント。1を超える場合はエラーメッセージを表示して保存を中止。

- **Pro**
  - 1スニペット内で使用できるプレースホルダ名の種類に **上限なし**。
  - 例：
    ```text
    あなたは {{role}} です。
    対象読者は {{audience}} です。
    {{topic}} について、{{language}} で説明してください。
    ```
    → `role` / `audience` / `topic` / `language` の 4種類すべて許可。

---

## 3. 検索・フィルタ・お気に入り

- **共通（Free / Pro 両方）**
  - 検索：タイトル / 本文 / タグ / カテゴリを対象にしたテキスト検索。
  - カテゴリフィルタ：カテゴリ単位での絞り込み。
  - お気に入りフラグ（★）：よく使うスニペットに印を付けて、ソートやフィルタに活用。
  - 右下パネルでのホバー preview：本文先頭 1〜2 行のツールチップ表示。

- **将来の拡張（Pro 候補）**
  - 並び替え（ドラッグ＆ドロップ）。
  - 「最近使った順」「使用頻度順」などスマートソート。

---

## 4. UI / レイアウト

- **共通**
  - popup.html：モダンなカードレイアウトの編集 UI。
  - contentScript パネル：右下にフローティングボタン＋黒系パネルでスニペット一覧。
  - ホバー時ツールチップにより、タイトルだけでなく本文の概要も確認可能。
  - テンプレート（`{{name}}`）については、
    - popup に "Template with blanks" ヘルプブロックと `+ Add placeholder` ボタンを用意済み。

- **Pro 専用 UI（将来）**
  - 並び替えハンドル（≡ アイコン）
  - バックアップ / エクスポートボタン
  - Pro バッジ表示（"Pro" ラベルなど）

---

## 5. データ・同期まわり

- **共通**
  - `chrome.storage.sync` によるスニペット保存（端末間同期）。
  - 基本的なデータ構造は Free / Pro 共通で、
    - `id`
    - `title`
    - `category`
    - `tags[]`
    - `body`
    - `favorite`
    - `createdAt` / `updatedAt`（必要に応じて）
    を想定。

- **Pro 拡張候補**
  - JSON エクスポート / インポート。
  - 外部ストレージやバックエンドとの連携。

---

## 6. 上限到達時の挙動（Free）

### 6.1 スニペット数上限（5件）

- 新規保存時に既存件数をカウント。
- 既に 5 件ある場合：
  - 保存を行わず、ポップアップ上部 or ダイアログでメッセージ表示。
  - メッセージ例：
    > **Free plan limit reached (5 snippets).**  
    > Delete an existing snippet or upgrade to add more.

### 6.2 プレースホルダ名の上限（1種類）

- スニペット保存時に `{{...}}` をパースしてプレースホルダ名を抽出。
- ユニークな名前数が 1 を超える場合：
  - 保存を行わず、メッセージ表示。
  - メッセージ例：
    > **Free plan supports 1 placeholder per snippet.**  
    > This snippet uses: "role", "topic".  
    > Keep one, or upgrade to use multiple placeholders.

---

## 7. まとめ

- **Free プランの目的**
  - 「スニペット管理の便利さ」と「プレースホルダ付きテンプレ」の価値を体験してもらう。
  - ただし、**スニペット数（5件）とプレースホルダの種類数（1種類）**に明確な上限を設け、
    本格的な活用や複雑なテンプレート構築は Pro に誘導する設計。

- **Pro プランの方向性**
  - 数量制限の撤廃（スニペット数・プレースホルダ種類数）。
  - 「整理・拡張・安全性（バックアップ）」を提供することで、
    日常的に ChatGPT を仕事で使うユーザーにとって必須レベルの価値を持つプランにする。

この仕様を元に、今後は `popup.js` / `contentScript.js` などに上限チェックと UI を実装していく。

